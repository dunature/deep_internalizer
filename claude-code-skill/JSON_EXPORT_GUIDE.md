# Deep Internalizer - JSON Export Guide

## 导出 JSON 格式

当用户要求导出分析结果时，使用以下格式生成 JSON 文件。

### JSON 结构

```json
{
  "taskId": "<UUID>",
  "source": "claude-code-skill",
  "title": "<文章标题>",
  "url": "<原始链接或本地路径>",
  "contentHash": "<SHA-256 哈希>",
  "status": "done",
  "createdAt": "<ISO 8601 时间戳>",
  "completedAt": "<ISO 8601 时间戳>",
  "model": "<使用的模型，如 glm-4.7>",
  "result": {
    "coreThesis": "<核心论点>",
    "summary": "<THESIS + OUTLINE 整合>",
    "chunks": [
      {
        "title": "<分块标题>",
        "summary": "<英文摘要>",
        "summary_zh": "<中文摘要>",
        "originalText": "<原文>",
        "startIndex": 0,
        "endIndex": 5
      }
    ],
    "vocabulary": [
      {
        "word": "<单词>",
        "phonetic": "/<音标>/",
        "pos": "<词性>",
        "slices": [
          {"text": "<音节>", "phonetic": "<音节 IPA>"}
        ],
        "definition": "<英文释义>",
        "definition_zh": "<中文释义>",
        "sentence": "<原句>",
        "newContext": "<新例句>"
      }
    ],
    "sentences": [
      {
        "text": "<原句>",
        "thoughtGroups": [
          {"text": "<意群>", "hint": "<提示>"}
        ],
        "translation": "<中文翻译>"
      }
    ]
  },
  "schemaVersion": "1.0.0"
}
```

## 导出方式

### 方式 1: 直接输出 JSON

在对话中直接输出 JSON，用户可以复制保存：

````json
{
  "taskId": "...",
  ...
}
````

### 方式 2: 保存到文件

将 JSON 保存到本地文件：

```bash
# 保存到当前目录
echo '<JSON 内容>' > deep-internalizer-export.json
```

### 方式 3: 提供下载链接

如果环境支持，生成可下载的文件链接。

## 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| `taskId` | ✅ | 唯一标识符，使用 UUID v4 |
| `source` | ✅ | 固定为 "claude-code-skill" |
| `title` | ✅ | 文章标题 |
| `url` | ✅ | 原始链接或本地文件路径 |
| `contentHash` | ⚠️ | 用于缓存检测，可省略 |
| `status` | ✅ | "done", "error", 或 "queued" |
| `model` | ⚠️ | 使用的 LLM 模型 |
| `result.coreThesis` | ✅ | 核心论点（一句话） |
| `result.summary` | ✅ | 综合摘要 |
| `result.chunks` | ✅ | 语义分块数组 |
| `result.vocabulary` | ⚠️ | 词汇数组（可选） |
| `result.sentences` | ⚠️ | 句子分析数组（可选） |

## 使用示例

### 在对话中导出

用户：`导出为 JSON`

回复：

```json
{
  "taskId": "abc123-def456",
  "source": "claude-code-skill",
  "title": "ASML's high-NA EUV tools...",
  ...
}
```

已生成 Deep Internalizer 格式的 JSON。你可以：
1. 复制上面的 JSON 内容
2. 保存为 `.json` 文件
3. 在 Deep Internalizer 中选择 Import → 粘贴 JSON 或选择文件

### 保存到文件

用户：`保存到文件`

回复：

```bash
# 已保存到当前目录
📁 deep-internalizer-asml-2026-02-28.json
```

## 注意事项

1. **字符转义**: JSON 中的引号、换行符需要正确转义
2. **UTF-8 编码**: 确保文件使用 UTF-8 编码，支持中文
3. **文件大小**: 如果内容过长，可以只导出核心字段（thesis + chunks）
4. **隐私**: 导出前确认内容不包含敏感信息

## 导入 Deep Internalizer

1. 打开 Deep Internalizer 应用
2. 点击 **Import** 按钮
3. 选择 **JSON Import**
4. 粘贴 JSON 内容或上传文件
5. 确认导入

导入后，内容将出现在学习列表中，可以进行 4 步深度学习循环。
